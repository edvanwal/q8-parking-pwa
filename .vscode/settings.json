{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": ".*",
        "cmd": "cd /d \"${workspaceFolder}\" && git add -A && git diff --staged --quiet || (git commit -m \"auto: update\" && git push)"
      }
    ],
    "autoShowOutputPanel": "error"
  }
}
